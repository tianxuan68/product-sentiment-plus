import { ArrowLeft, ArrowRight, LockKeyhole } from 'lucide-react';
import { FormEvent, useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { ActionButton } from '../components/ActionButton';
import { login } from '../services/backendService';
export function Login() { const navigate = useNavigate(); const [username, setUsername] = useState('dxx'); const [password, setPassword] = useState('dxx123456@'); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); async function submit(event: FormEvent) { event.preventDefault(); setBusy(true); setError(''); try { await login(username, password); navigate('/dashboard'); } catch (e) { setError(e instanceof Error ? e.message : '登录失败'); } finally { setBusy(false); } } return <div className="auth-page"><Link to="/" className="auth-back"><ArrowLeft size={16} /> 返回首页</Link><div className="auth-layout"><div className="auth-message"><span className="section-kicker">Welcome back</span><h1>继续听见<br /><em>用户的声音。</em></h1><p>你的洞察空间，已经准备好了。</p></div><form className="auth-card" onSubmit={submit}><div className="auth-card-head"><span className="brand-orb">✦</span><div><h2>欢迎回来</h2><p>登录你的澄见空间</p></div></div><label>账号<input value={username} onChange={e => setUsername(e.target.value)} autoComplete="username" /></label><label>密码<input type="password" value={password} onChange={e => setPassword(e.target.value)} autoComplete="current-password" /></label>{error && <div className="form-error">{error}</div>}<ActionButton type="submit" disabled={busy} className="auth-submit">{busy ? '正在进入…' : '登录并进入空间'} <ArrowRight size={17} /></ActionButton><div className="demo-tip"><LockKeyhole size={15} /> 演示账号已预填，可直接登录</div></form></div></div>; }
